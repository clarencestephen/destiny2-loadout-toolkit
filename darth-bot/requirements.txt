# Darth Bot — Destiny 2 chatbot for the DARTH_BANKAI Discord
# Runs as a Discord bot, answers Destiny questions via Qwen 3 8B + RAG.

# Discord
discord.py>=2.4

# LLM (Qwen 3 via ollama — install ollama separately: https://ollama.com)
ollama>=0.4
httpx>=0.27

# RAG stack
chromadb>=0.5
sentence-transformers>=3.0   # embeddings (BAAI/bge-small or all-MiniLM)

# Scraping
httpx[http2]
beautifulsoup4>=4.12
lxml>=5.0
trafilatura>=1.12            # clean article extraction

# Search
# (Brave Search API — no library needed, plain httpx call)

# Utilities
python-dotenv>=1.0
pydantic>=2.0
